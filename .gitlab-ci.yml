
include:
    - 'templates/.register.yml'
    - 'templates/.deploy.yml'
    
stages:
    - register
    - deploy

default:
    artifacts:
        paths:
            - package-lock.json
            - node_modules
            - build
            - .docusaurus
